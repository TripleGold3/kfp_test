apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: trame-app-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: trame-app-service
            port:
              number: 80
